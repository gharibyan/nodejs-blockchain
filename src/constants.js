const PEER_DEFAULT_CONFIGS = {
  timeout: 10000,
  retry: 20
}

const LINK_HOST_URL = 'http://127.0.0.1:30001' // this also can be as env configs

module.exports = {
  PEER_DEFAULT_CONFIGS,
  LINK_HOST_URL
}
